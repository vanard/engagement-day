`kisah-romantis-clip.mp3` is a 35-second excerpt (01:29–02:04) of the
user-supplied Glenn Fredly — Kisah Romantis MP3, encoded as 128 kbps stereo
(about 561 KB). The original file is unchanged. No fades were added.

The source is configured in `src/content/invitation.ts`. Playback starts from
the opening gesture, loops, and uses `preload="none"`.
